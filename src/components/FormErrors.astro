---
interface Props {
  errors?: string[]
}

const { errors } = Astro.props

if (!errors || errors.length === 0) {
  return
}
---

<div>
  <h2>Errors</h2>
  <ul>
    {errors.map((error) => <li>{error}</li>)}
  </ul>
</div>
